name: guidance_counselor_module
description: Guidance Counselor dashboard shell — dropout-risk analytics and approval queue.
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  google_fonts: ^6.2.1
  file_picker: ^11.0.2
  discipline_officer_module:
    path: ../discipline_officer_module
  dashboard_layout:
    path: ../dashboard_layout

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
